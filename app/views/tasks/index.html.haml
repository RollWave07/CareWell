%h1
  / can add glyphicons like so below:
  / %span{:class => "glyphicon glyphicon-saved"}
  Upcoming Tasks

-if @tasks.empty?
  ="Add New"
-else
  -@tasks.each do |task|
    .task
      %ul
        %li=task.task_date.strftime("%A, %m/%d/%Y")
        / =task.task_date.strftime("%b") unless task_date.nil?
        / =task.task_date.strftime("%m/%d/%Y") unless task_date.nil?
        %li=task.category
        %li=task.title
        -if task.assignee_id.nil? && task.user_id == current_user.id
          %li= render 'tasks/invite'
        -else task.assignee_id.nil?
          %li=link_to "Sign-Up", edit_group_task_path(@group, task.id), :class => 'sign-up'

        %li=link_to "Edit", edit_group_task_path(@group, task.id)



  =link_to "Add New", new_group_task_path(@group)





