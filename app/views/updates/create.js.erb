
(function () {
  var commentHtml = "<%= j render(partial: 'update', locals: {update: @update})%>";
  $('.updates[data-id="<%=@update.task.id%>"]').append(commentHtml).on("ajax:success", function(e, data, status, xhr){
      console.log(data)
      $("#vote" + data.vote_id).text(data.count);
      console.log("#vote" + data.vote_id)
    });
;
  $('#update_comment').val('');
  // $(".like-button").on("click", function (e) {
  //   e.preventDefault();
  //   console.log("click");
  //   $(".like-counter").text("Likes: @update.votes.count");
  // });
})();


