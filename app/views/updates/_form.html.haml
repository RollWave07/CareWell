=form_for [@group, @update], remote: true do |f|
  .field
    =f.label :comment
    =f.text_field :comment, size: '60'
  .field
    =f.hidden_field(:task_id, value: task.id)
  .field
    =f.hidden_field(:picture)  
  .action
    =f.submit "Add Update"