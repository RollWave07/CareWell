%h1 Users#show
%p Find me in app/views/users/show.html.haml
- @users.each do |user|
  
end 

t.string :first_name
      t.string :last_name
      t.string :phone
      t.string :picture
      t.string :role
      t.boolean :admin
      t.text :notes
      t.references :group, index: true